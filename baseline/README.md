# Legacy Baseline

该目录是迁移前的历史目录。当前可运行入口已经移到：

```text
models/logistic/train.py
experiments/high_pain_augmentation/tune_logistic_high_pain.py
```

使用 pixi：

```bash
pixi run logistic-train
pixi run hp-logistic-tune
```

历史输出和缓存未在本次整理中删除。
